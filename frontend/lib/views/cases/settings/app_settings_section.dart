import 'package:settings_ui/settings_ui.dart';

class AppSettingsSection extends SettingsSection {
  const AppSettingsSection({
    super.key,
    required super.tiles,
    super.margin,
    super.title,
  });
}
